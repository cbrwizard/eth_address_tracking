/*
 * Is responsible for storing the whole app's initial frontend state.
 */
const initialState = {
  day: {
    canVoteToday: false,
    isLoading: false,
    record: {},
  },
}

export default initialState
